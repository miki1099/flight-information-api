spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/airport_flight_info?serverTimezone=CET
      username: root
      password: root1234
      driverClassName: com.mysql.cj.jdbc.Driver
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.MySQL5Dialect
      dll-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    generate-ddl: true
  messages:
    basename: messages/messages
server:
  port: 8081
